# Override for production builds only (dev auto-detects localhost:8000)
VITE_API_BASE_URL=https://api.custosell.com/api/v1

VITE_APP_VERSION=1.0.0


1. On the Card/Lead on the kanban column, lets add the message icon(to signify comments on that card/lead with count superscripted, where user when he/she clicks on that comment icon, he can  be able to read others comment and also add theirs, this should not trigger the whole lead/card/display to show 3.On the card/lead when it has been assigned, lets had something like a link between the two avatars from assigner to assignee(s).Handle this with design accuracy and precision(use a simple modal for comments)Lets keep the main lead/card model, no need to chnage/remove anything where users make lists,labels,etc.3. Now, when say i am a business owner, can i ba able to give my staff/user access to al sub nav items unde 'Projects & Estimates' and be able to revoke access? without losing current functionality where normal access only see 'Projects & Estimates'? lets enforce that, because later on, i want project managers or dedicated individuals to be able to manage proects,estimates,costs,etc while i also have control as well, do not break anthing which already exists.