/** Shared shell chrome - sidebar brand strip and navbar use the same height so borders align. */
export const SHELL_HEADER_HEIGHT_CLASS = 'h-16';

/** Sidebar widths - keep in sync with Sidebar.tsx */
export const SIDEBAR_WIDTH_EXPANDED_CLASS = 'lg:left-[247px]';
export const SIDEBAR_WIDTH_COLLAPSED_CLASS = 'lg:left-[64px]';

/** Auth mobile bottom tab bar - height + safe-area. Hide bar at `lg` (1024). In-flow in Layout (not fixed). */
export const APP_MOBILE_TAB_BAR_HEIGHT_CLASS = 'h-16';
